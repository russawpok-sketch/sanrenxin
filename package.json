{
  "name": "livestock-experience",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "dev:host": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc && vite build",
    "build:pages": "tsc && vite build --base /cursor/",
    "preview": "vite preview --host 127.0.0.1 --port 3000",
    "sprites:knockout": "node scripts/knockout-sprites.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
